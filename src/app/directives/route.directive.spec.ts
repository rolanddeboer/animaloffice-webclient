import { RouteDirective } from './route.directive';

describe('RouteDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteDirective();
    expect(directive).toBeTruthy();
  });
});
