import { NoDoubleClickDirective } from './no-double-click.directive';

describe('NoDoubleClickDirective', () => {
  it('should create an instance', () => {
    const directive = new NoDoubleClickDirective();
    expect(directive).toBeTruthy();
  });
});
